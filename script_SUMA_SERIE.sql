CREATE TABLE SUMA_SERIE (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    VALOR_N NUMBER NOT NULL,
    PROCEDIMIENTO VARCHAR2(4000),
    RESULTADO NUMBER,
    FECHA_REGISTRO DATE DEFAULT SYSDATE
);

COMMIT;

SELECT * FROM SUMA_SERIE;